{
  "name": "@ai-workhub/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for AI-WorkHub",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
